{
  "name": "halfpen",
  "version": "0.1.0",
  "description": "Half-Pen",
  "author": "Graighle",
  "license": "MIT",
  "scripts": {
    "client": "cd client && npm run start",
    "server": "cd server && npm run start",
    "dev": "SET DEBUG=halfpen-server:* & concurrently --kill-others-on-fail \"npm run client\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
